@model string[]

<span class="theme-colors">
    @for (int i = 0; i < Model.Length; i++)
    {
        if (@Model.Length == 3 || i%2 ==0) {
            <span class="color-box" style="background-color: @Model[i]"></span>
        }
    }
</span>
