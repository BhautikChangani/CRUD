<div class="demo-section wide">
    @(Html.Kendo().Chart<Kendo.Mvc.Examples.Models.BudgetReportItem>()
        .Name("chart")
        .Title("Budget report")
        .Legend(legend => legend
            .Position(ChartLegendPosition.Right)
        )
        .SeriesDefaults(seriesDefaults => seriesDefaults.RadarLine().Style(ChartRadarLineStyle.Smooth))
        .DataSource(ds => ds.Read(read => read.Action("Smooth_BudgetReport", "Radar_Charts")))
        .Series(series => {
            series.RadarLine(model => model.Budget).CategoryField("Unit");
            series.RadarLine(model => model.Spending).CategoryField("Unit");
        })
        .ValueAxis(axis => axis.Numeric()
            .Labels(labels => labels.Template("$#= value / 1000 #k"))
        )
    ) 
</div>